#include <bits/stdc++.h>

using namespace std;

vector<int> flip(string A)
{
    int s = -1, e = -1;
    int count = 1;
    for (int i = 0; i < A.size(); i++)
    {
        if (s == -1 && A[i] == '0')
            s = i;
        else
        {
            
        }
    }
}
