#include <bits/stdc++.h>

using namespace std;

int solve(int n, int s)
{
    vector<vector<int>> dp(n + 1, vector<int>(s + 1));

    for (int i = 0; i <= n; i++)
    {
        for (int j = 0; j <= s; j++)
        {
            if(j == 0)
            {
                
            }
        }
    }
}
