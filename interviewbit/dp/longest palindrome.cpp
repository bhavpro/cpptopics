#include <bits/stdc++.h>

using namespace std;

string longestPalindrome(string A)
{
    int dp[1000][1000];
    for (int i = 0; i < A.size(); i++)
    {
        for (int j = 0; j <= A.size(); j++)
        {
            dp[i][j] = dp[i+1]
        }
    }
}