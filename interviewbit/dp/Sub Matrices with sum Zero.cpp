#include <bits/stdc++.h>

using namespace std;

int solve(vector<vector<int>> &A)
{
    int m = A.size();
    int n = A[0].size();
    vector<int> rowsum(m), colsum(n);
    vector<vector<int>> matsum;
    for (int i = 0)
}
