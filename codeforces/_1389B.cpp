#include <bits/stdc++.h>

using namespace std;

int solve(int n, int k, int z)
{
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    freopen("input.txt", "r", stdin);
    int t, n, k, z;
    cin >> t;
    while (t--)
    {
        cin >> n >> k >> z;
        cout << solve(n, k, z) << "\n";
    }
}